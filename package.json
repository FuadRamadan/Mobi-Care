{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "preinstall": "sh -c 'rm -f package-lock.json yarn.lock; case \"$npm_config_user_agent\" in pnpm/*) ;; *) echo \"Use pnpm instead\" >&2; exit 1 ;; esac'",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck",
    "build:production": "pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/mobicare-gateway run build && pnpm --filter @workspace/pharmacy-portal run build",
    "start": "pnpm --filter @workspace/api-server run start",
    "migrate": "pnpm --filter @workspace/db run migrate"
  },
  "engines": {
    "node": ">=22 <25",
    "pnpm": ">=10 <11"
  },
  "private": true,
  "dependencies": {
    "@replit/connectors-sdk": "^0.4.1"
  },
  "devDependencies": {
    "prettier": "^3.9.6",
    "typescript": "~5.9.3"
  }
}
